stages:
  - quality

quality:
  stage: quality
  tags: []
  script:
    - pwsh -NoProfile -Command "Install-Module Pester -RequiredVersion 4.10.1 -Scope CurrentUser -Force -SkipPublisherCheck"
    - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/quality/run-local-quality-gates.ps1 -Profile full
