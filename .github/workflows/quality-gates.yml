name: quality-gates

on:
  pull_request:
  push:
    branches: [ main, master ]

jobs:
  quality:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run local quality gates (if exists)
        shell: pwsh
        run: |
          if (Test-Path "scripts/quality/run-local-quality-gates.ps1") {
            powershell -File scripts/quality/run-local-quality-gates.ps1 -Profile quick
          } else {
            Write-Host "No project quality script found, skip."
          }

      - name: Run supply-chain checks (if exists)
        shell: pwsh
        run: |
          if (Test-Path "scripts/quality/run-supply-chain-checks.ps1") {
            powershell -File scripts/quality/run-supply-chain-checks.ps1 -Profile quick
          } else {
            Write-Host "No supply-chain script found, skip."
          }

      - name: Check waiver expiry (if exists)
        shell: pwsh
        run: |
          if (Test-Path "scripts/quality/check-waivers.ps1") {
            powershell -File scripts/quality/check-waivers.ps1
          } else {
            Write-Host "No waiver-check script found, skip."
          }
