interface:
  display_name: "Capability Router"
  short_description: "Discover cold capabilities and apply deterministic safety policy"
  default_prompt: "Use $capability-router only when no visible capability matches or cross-profile discovery is requested. Let the host AI choose from profile-scoped candidates, then validate the chosen capability without changing host state."
policy:
  allow_implicit_invocation: true
