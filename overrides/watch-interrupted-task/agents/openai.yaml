interface:
  display_name: "任务连续性守夜"
  short_description: "静默恢复中断任务，以硬钩子阻断跨任务消息注入"
  default_prompt: "Use $watch-interrupted-task to silently resume interrupted Codex tasks with hard cross-thread send protection."
