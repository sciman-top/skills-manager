interface:
  display_name: "任务连续性守夜"
  short_description: "自动恢复中断任务并监督 Goal 验收、纠偏与停止"
  default_prompt: "Use $watch-interrupted-task to supervise this Goal, recover interrupted work from verified checkpoints, and stop only after acceptance evidence passes."
