interface:
  display_name: "Capability Router"
  short_description: "Discover cold capabilities and apply deterministic safety policy"
  default_prompt: "Use $capability-router only when no visible matching skill or callable native tool covers the request. Let the host AI choose from portable domain-scoped candidates, then validate the chosen capability without changing host state."
policy:
  allow_implicit_invocation: true
