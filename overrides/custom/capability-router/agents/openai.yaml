interface:
  display_name: "Capability Router"
  short_description: "Validate host-selected skill metadata against local policy"
  default_prompt: "Use $capability-router only when visible metadata is insufficient or deterministic policy validation is required; keep host AI semantic selection and negative constraints authoritative, and never use it as middleware, switch profiles, or mutate host state."
policy:
  allow_implicit_invocation: true
