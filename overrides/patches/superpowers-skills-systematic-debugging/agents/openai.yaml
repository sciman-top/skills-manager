interface:
  display_name: "Focused Debugging"
  short_description: "Find and fix one observed root cause"
  default_prompt: "Use $systematic-debugging to isolate the observed failure, fix its causal seam, and stop after the lowest sufficient verification passes."
policy:
  allow_implicit_invocation: true
