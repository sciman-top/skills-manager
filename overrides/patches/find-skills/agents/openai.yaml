interface:
  display_name: "Find Skills"
  short_description: "Search and assess installable skills safely"
  default_prompt: "Use $find-skills to search read-only, verify upstream fit and overlap, and do not install anything."
policy:
  allow_implicit_invocation: true
